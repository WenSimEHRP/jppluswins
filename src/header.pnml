grf {
        grfid: "WS\24\05";
        name: string(STR_GRF_NAME);
        desc: string(STR_GRF_DESC);
        version: 2;
        min_compatible_version: 2;
}

if (grf_future_status("\45\53\05\00") == 1) {
        error(ERROR, string(STR_GRF_JAPANSET));
}
