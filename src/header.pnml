grf {
        grfid: "WS\01\01";
        name: string(STR_GRF_NAME);
        desc: string(STR_GRF_DESC);
        version: 0;
        min_compatible_version: 0;
    }

if (grf_future_status("SZ\0D\00") == 1) {
        error(ERROR, string(STR_GRF_JAPANSET));
}
